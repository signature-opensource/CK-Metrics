An InfluxDB export consumer for the CK.AppIdentity metrics pipeline.

Posts measurements to InfluxDB v2.x over HTTP(S) using the line protocol. Enabled and configured from
the AppIdentity local configuration - bucket, organization and token included.
