A CSV export consumer for the CK.AppIdentity metrics pipeline.

Writes measurements to a CSV file, for analysis in a spreadsheet or ingestion by another tool. Enabled
and configured from the AppIdentity local configuration - there is no code to write.
